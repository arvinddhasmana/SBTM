apiVersion: v1
kind: Service
metadata:
  name: compliance-management
spec:
  selector:
    app: compliance-management
  ports:
    - name: http
      port: 80
      targetPort: 3005
  type: ClusterIP
