apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: emergency-alerts
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: emergency-alerts
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
