apiVersion: apps/v1
kind: Deployment
metadata:
  name: emergency-alerts
  labels:
    app: emergency-alerts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: emergency-alerts
  template:
    metadata:
      labels:
        app: emergency-alerts
      annotations:
        azure.workload.identity/use: 'true'
    spec:
      serviceAccountName: sbtm-workload-sa
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: sbtm-keyvault-secrets
      containers:
        - name: emergency-alerts
          image: sbtmacrdemo.azurecr.io/sbtm/emergency-alerts:latest
          ports:
            - containerPort: 3002
          envFrom:
            - secretRef:
                name: sbtm-secrets
          env:
            - name: PORT
              value: '3002'
            - name: NODE_ENV
              value: 'production'
          volumeMounts:
            - name: secrets-store
              mountPath: /mnt/secrets-store
              readOnly: true
          resources:
            requests:
              cpu: '250m'
              memory: '256Mi'
            limits:
              cpu: '1'
              memory: '512Mi'
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
