apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-presence
  labels:
    app: student-presence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: student-presence
  template:
    metadata:
      labels:
        app: student-presence
        azure.workload.identity/use: 'true'
    spec:
      serviceAccountName: sbtm-workload-sa
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: sbtm-keyvault-secrets
      containers:
        - name: student-presence
          image: sbtmacrdemo.azurecr.io/sbtm/student-presence:latest
          ports:
            - containerPort: 3003
          envFrom:
            - secretRef:
                name: sbtm-secrets
          env:
            - name: PORT
              value: '3003'
            - name: NODE_ENV
              value: 'production'
          volumeMounts:
            - name: secrets-store
              mountPath: /mnt/secrets-store
              readOnly: true
          resources:
            requests:
              cpu: '250m'
              memory: '256Mi'
            limits:
              cpu: '1'
              memory: '512Mi'
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 10
