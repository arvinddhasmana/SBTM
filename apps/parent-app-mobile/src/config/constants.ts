/** Polling and timeout constants for the Parent App. */

/** Bus location poll interval (ms) on the map screen. */
export const BUS_LOCATION_POLL_MS = 5_000;

/** Active alerts poll interval (ms) on the dashboard. */
export const ALERT_POLL_INTERVAL_MS = 15_000;

/** Dashboard live-location refresh (ms) for deriving on_bus status. */
export const DASHBOARD_LIVE_LOCATION_POLL_MS = 15_000;

/** HTTP request timeout (ms). */
export const API_REQUEST_TIMEOUT_MS = 30_000;
